{
  "name": "@vocion/sdk",
  "type": "module",
  "version": "0.3.0",
  "description": "Stable plugin contract for Vocion — Operation + Source + PluginManifest + LLM client types. v0.3: adds Source plugin contract (OAuth2 + API key, org + user scope, pull + retrieve + healthCheck) for connector authors.",
  "license": "Apache-2.0",
  "keywords": [
    "vocion",
    "ai",
    "agents",
    "plugin",
    "sdk"
  ],
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "README.md",
    "src"
  ],
  "peerDependencies": {
    "openai": "^6.0.0",
    "zod": "^4.0.0"
  }
}
