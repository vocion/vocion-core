# Guide, a nav row rather than a rendered table. Operating intent is a file in
# the workspace, not rows in a database, so the page that renders it is a core
# page at /dashboard/guide that reads operating-intent.yaml and proposes edits
# on the action rail. This seats it in Business, fourth, beside the surfaces a
# person steers from.
slug: guide
title: Guide
description: >-
  What you want the factory doing next. Outcomes, what beats what, what it may
  not do without asking, what it may spend, and the judgment it cannot derive
  from records.
icon: compass
nav: {section: Business, order: 4}
archetype: link
href: /dashboard/guide
