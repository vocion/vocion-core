/**
 * The decision bar of the Detail archetype. One ink primary, ghost
 * secondaries, a collapsed field. Lives in `features/dashboard` (PR #337)
 * and is re-exported here so pages reach every pattern from one path.
 */
export { type BarAction, type BarField, StickyActionBar } from '@/features/dashboard/StickyActionBar';
