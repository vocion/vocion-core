{
  "name": "@vocion/agent-runtime",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Vocion BYOA agent runtime — the deepagents loop as a standalone artifact (/invocations + /ping), hostable on AWS Bedrock AgentCore Runtime or any container/host.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "node esbuild.config.mjs",
    "check:types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agentcore": "^3.1122.0",
    "@langchain/anthropic": "^1.5.8",
    "@langchain/aws": "^1.4.4",
    "@langchain/core": "^1.1.45",
    "deepagents": "^1.13.2",
    "langfuse": "^3.38.20"
  },
  "devDependencies": {
    "esbuild": "~0.28.2",
    "tsx": "^4.23.13",
    "vitest": "^4.1.11"
  }
}
